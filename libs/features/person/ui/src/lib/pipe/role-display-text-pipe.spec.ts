import { RoleDisplayTextPipe } from './role-display-text-pipe';

describe('RoleDisplayTextPipe', () => {
  it('create an instance', () => {
    const pipe = new RoleDisplayTextPipe();
    expect(pipe).toBeTruthy();
  });
});
